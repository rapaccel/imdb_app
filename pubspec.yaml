name: imdb_app
description: "A new Flutter project."
publish_to: "none"
version: 0.1.0

environment:
  sdk: ^3.8.0

dependencies:
  flutter:
    sdk: flutter
  dio: ^5.8.0+1
  get_it: ^8.1.0
  sqflite: ^2.4.2
  flutter_bloc: ^9.1.1
  freezed_annotation: ^3.1.0
  equatable: ^2.0.7
  dartz: ^0.10.1
  build_runner: ^2.6.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  freezed: ^3.1.0

flutter:
  uses-material-design: true
